<%= form_with(model: maritime_term, local: true) do |form| %>
  <% if maritime_term.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(maritime_term.errors.count, "error") %> prohibited this maritime_term from being saved:</h2>

      <ul>
        <% maritime_term.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :term %>
    <%= form.text_field :term %>
  </div>

  <div class="field">
    <%= form.label :explination %>
    <%= form.text_area :explination %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
